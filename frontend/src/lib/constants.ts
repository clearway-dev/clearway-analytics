export const PASSABILITY_THRESHOLD_M = 3.0;
export const PASSABILITY_THRESHOLD_CM = 300;
// Default vehicle width shown in the UI on first load.
// Intentionally lower than PASSABILITY_THRESHOLD_CM — represents a realistic
// emergency vehicle, not the absolute passability limit.
export const DEFAULT_VEHICLE_WIDTH_CM = 250;
